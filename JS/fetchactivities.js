var events = [];var months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];function capitalizeFirstLetter(string) {    return string.charAt(0).toUpperCase() + string.slice(1);}fetch('../PHP/activity.php')    .then(function(response) {    return response.json()  }).then(function(json) {    console.log('parsed json', json);    for(var i = 0; i<json.length;i++){        console.log(json[i].activity);        var string = json[i].date;        var datearray = string.split('-');        var currentmonth = parseInt(datearray[1]) - 1;        console.log(months[currentmonth]);        var calendarEvent = $('<div />', { class: 'calendar_event'});        var calendarList = $('<ul />');        calendarEvent.append(calendarList);        calendarList.append("<li class='calendar_event_date'><div class='calendar_event_date_div'><span class='calendar_event_date_day'>"+datearray[2]+"</span><span class='calendar_event_date_yearmonth'><span class='calendar_event_date_month'>"+months[currentmonth]+"</span><span class='calendar_event_date_year'>"+datearray[0]+"</span></span></div></li>");        var eventName = $('<li />', { class: "calendar_event_name", text: capitalizeFirstLetter(json[i].activity) });        calendarList.append(eventName);        var eventLocation = $('<li />', { class: "calendar_event_location", text: capitalizeFirstLetter(json[i].location)});        calendarList.append(eventLocation);        $('#calendar_container').append(calendarEvent);    }  }).catch(function(ex) {    console.log('parsing failed', ex)  })